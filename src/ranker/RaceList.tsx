import React from 'react';
import PageFlow from '../UI/PageFlow';

interface Props {}
interface State {}

export default class RaceList extends React.Component<Props, State> {
  public render() {
    return (
      <PageFlow>
        <div>race list go here</div>
      </PageFlow>
    );
  }
}
