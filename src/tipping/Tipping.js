// @flow
import React from 'react';
import MenuBar from "../common/MenuBar";

function Tipping() {
    return (
        <div>
            <MenuBar title="Tipstar" />
        </div>
    );
}

export default Tipping;